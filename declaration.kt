fun main(){
    var name : String = "Hello"
    println(name)
    // var can be changed but data type cannot 
    name = "World"
    println(name)
    // name = 3 // error
   // print(name)

   // val is also used to declare values but neither values nor data type can be changed
    val pi = 3.143
    println(pi)
    // pi = 3 is error
}